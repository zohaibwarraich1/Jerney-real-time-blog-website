apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: jerney-ns
  labels:
    app: jerney-backend
    environment: prod
  annotations:
    description: "General configuration for the Jerney backend"
data:
  PORT: "5000"
  DB_HOST: "database-service"
  DB_PORT: "5432"